Perform a full end-to-end audit of the entire LyraPrep application and fix every bug, broken flow, and inconsistency found. Do not add new features. Do not refactor working code. Only find, log, and fix what is broken. Work through the application systematically in the order below — do not skip sections.
1. Authentication and session layer

Verify login works correctly for all three roles: Teacher, Parent, Student
Confirm the session correctly identifies the logged-in user's role and ID on every page load
Check that protected routes redirect unauthenticated users to login and do not flash protected content before redirecting
Confirm that logging out fully clears the session and does not leave stale role or user data in memory or local storage
Verify that a Teacher session cannot access Student or Parent-only routes and vice versa

2. Navigation and routing

Confirm every sidebar nav item for all three roles routes to the correct page without errors
Verify that back navigation on every drill-down page (Assignment detail, Subject subfolder, Student profile, Material detail) returns the user to the correct previous page — not a hardcoded fallback
Check that direct URL access to any route works correctly and does not break when the page is refreshed
Confirm that no route renders a blank page, infinite loader, or uncaught error for any role

3. Classrooms and folder system

Verify a Teacher can create a grade folder, add subject subfolders, and enroll students without errors
Confirm enrolled students see the correct grade folders and subjects under My Classes in their sidebar
Confirm parents see the correct classrooms filtered to their child's enrollments under Classes
Verify that clicking into a grade folder and then a subject subfolder works for all three roles
Check that all four tabs inside a subject subfolder — Assignments, Study Materials, Feed, and Grades — load correctly for Teacher, Student, and Parent sessions
Confirm that a student cannot see subject subfolders they are not enrolled in

4. Assignments

Verify a Teacher can create an Assignment with all required fields including the Type dropdown (Assignment, Test, Quiz, Project) without errors
Confirm the Assignment appears in the correct subject subfolder for enrolled students immediately after creation
Verify a Student can open, complete, and submit an Assignment without errors
Confirm the Teacher can open Grade Submissions, view the student's submission, and enter a grade
Check that the graded score appears correctly in the student's and parent's Grades tab with the correct type weighting applied
Verify that unsaved changes on the Assignment editor trigger the in-app warning dialog — not a browser pop-up

5. Study materials

Verify a Teacher can upload or link a Study Material inside a subject subfolder
Confirm the material appears for all enrolled students in that subject
Check that students and parents can open and view the material without errors

6. Grading and grade display

Verify the Grading Policy settings panel inside each subject loads correctly for the Teacher
Confirm that saving a policy with weights that do not sum to 100% shows an inline validation error and blocks saving
Confirm that a valid policy saves correctly and a new policy record is created with today's effective date
Verify the Grades tab shows the correct weighted overall percentage and per-type breakdown for Teacher, Student, and Parent
Confirm that types with no graded items show as "Pending" in amber and are excluded from the overall percentage
Verify the "Based on graded items only — [X] pending" note appears when any type is pending

7. Messaging

Verify the three-way thread loads correctly for Teacher, Student, and Parent sessions
Confirm a message sent by any party appears in all three views on next load
Check that the empty state renders correctly when a thread has no messages — no blank screen or error
Verify the composer sends messages without errors and the thread updates immediately after sending

8. Notifications

Confirm that no browser-native alerts, confirms, or beforeunload pop-ups appear anywhere in the application
Verify that all success, error, warning, and info messages render as the unified in-app notification from the top center of the screen
Check that success and info notifications auto-dismiss after 4 seconds
Confirm that error and warning notifications persist until manually dismissed
Verify all notification copy is plain English with no raw error strings or stack traces exposed to the user

9. Forms and validation

Audit every form across all three role dashboards and confirm that required fields block submission when empty
Verify inline validation errors appear next to the relevant field — not as a pop-up or page-level alert
Confirm that every form submit button shows a loading state while the request is in flight and re-enables correctly after success or failure

10. Empty states

Verify that every list, table, tab, and feed across the entire application has a friendly empty state when there is no data — no blank space, null renders, or undefined errors visible to the user
Check this for: Assignments list, Study Materials list, Grade Submissions, Students list, Classrooms list, Messages thread, Grades tab, Feed tab, and Progress Reports

11. Responsive layout

Confirm the sidebar, topbar, and main content area render correctly at desktop width
Check that no content overflows its container, overlaps another element, or becomes unreadable at standard screen sizes
Verify that modals and dialogs are centered and do not render off-screen

After completing the audit:

List every bug found with a one-line description and where it was found
Confirm every fix was applied and tested in the same session
Flag anything that could not be fixed and explain why